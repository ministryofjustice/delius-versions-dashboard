export interface EnvironmentConfig {
  name: string;
  data: string;
  infrastructure: string;
  env_config: string;
  delius: string;
  rbac: string;
  umt: string;
  'gdpr (api/ui)': string;
  'merge (api/ui)': string;
  'oracle (delius/mis)': string;
}
